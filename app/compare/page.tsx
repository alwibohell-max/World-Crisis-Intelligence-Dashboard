import { getCountry } from "@/lib/api/countries";
import { CompareRadarChart } from "@/components/charts/CompareRadarChart";
export default async function ComparePage() { const countries = await Promise.all(["Japan", "Chile", "Türkiye"].map(getCountry)); const rows = countries.map((r) => r.data[0]).filter(Boolean); return <div className="space-y-6"><h1 className="text-3xl font-bold">Compare Regions</h1><CompareRadarChart data={rows.map((c) => ({ metric: c.name, risk: c.risk.score, population: c.population / 1_000_000 }))} /><div className="overflow-x-auto rounded-lg border"><table className="w-full text-sm"><tbody>{rows.map((c) => <tr key={c.cca3} className="border-t"><td className="p-3 font-semibold">{c.name}</td><td className="p-3">{c.region}</td><td className="p-3">{c.population.toLocaleString()}</td><td className="p-3">Risk {c.risk.score}</td></tr>)}</tbody></table></div></div>; }
