import { getLatestRates, getRateTimeseries } from "@/lib/api/currency";
import { MetricCard } from "@/components/cards/MetricCard";
export default async function CurrencyPage() { const [rates, trend] = await Promise.all([getLatestRates("USD"), getRateTimeseries("USD", "EUR")]); const top = Object.entries(rates.data.rates).slice(0,8); return <div className="space-y-6"><h1 className="text-3xl font-bold">Currency & Economy Signals</h1><div className="grid gap-4 md:grid-cols-4">{top.map(([code, rate]) => <MetricCard key={code} label={`USD → ${code}`} value={rate.toFixed(3)} detail={rates.data.date} />)}</div><div className="rounded-lg border p-4"><h2 className="font-semibold">30-day USD/EUR trend</h2><pre className="mt-3 max-h-60 overflow-auto text-xs text-muted-foreground">{JSON.stringify(trend.data.slice(-10), null, 2)}</pre></div></div>; }
