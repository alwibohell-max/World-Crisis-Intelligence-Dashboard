"use client";
import { useTheme } from "next-themes";
import { Button } from "@/components/ui/button";
import { useAppStore } from "@/store/useAppStore";
export default function SettingsPage() { const { setTheme } = useTheme(); const clear = useAppStore((s) => s.clearAll); const units = useAppStore((s) => s.units); const setUnits = useAppStore((s) => s.setUnits); return <div className="space-y-6"><h1 className="text-3xl font-bold">Settings</h1><section className="rounded-lg border p-4"><h2 className="font-semibold">Theme</h2><div className="mt-3 flex gap-2"><Button onClick={() => setTheme("dark")}>Dark</Button><Button variant="outline" onClick={() => setTheme("light")}>Light</Button><Button variant="outline" onClick={() => setTheme("system")}>System</Button></div></section><section className="rounded-lg border p-4"><h2 className="font-semibold">Unit settings</h2><div className="mt-3 flex flex-wrap gap-2"><Button variant={units.temperature === "c" ? "default" : "outline"} onClick={() => setUnits({ ...units, temperature: "c" })}>Celsius</Button><Button variant={units.temperature === "f" ? "default" : "outline"} onClick={() => setUnits({ ...units, temperature: "f" })}>Fahrenheit</Button></div></section><section className="rounded-lg border p-4"><h2 className="font-semibold">Storage</h2><Button variant="destructive" className="mt-3" onClick={clear}>Clear localStorage</Button></section></div>; }
