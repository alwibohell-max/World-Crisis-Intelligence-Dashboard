import { Badge } from "@/components/ui/badge";
export function SourceBadge({ source }: { source: string }) { return <Badge className="border-cyan-500/30 bg-cyan-500/10 text-cyan-200">{source}</Badge>; }
