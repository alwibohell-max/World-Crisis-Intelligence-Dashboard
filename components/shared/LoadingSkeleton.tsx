export function LoadingSkeleton({ lines = 4 }: { lines?: number }) { return <div className="space-y-3">{Array.from({ length: lines }).map((_, i) => <div key={i} className="h-4 animate-pulse rounded bg-muted" style={{ width: `${95 - i * 12}%` }} />)}</div>; }
