import type { EarthquakeEvent } from "@/lib/types";
import { RiskBadge } from "@/components/shared/RiskBadge";
import { formatDate } from "@/lib/utils";
export function EarthquakeTable({ events }: { events: EarthquakeEvent[] }) { return <div className="overflow-x-auto rounded-lg border"><table className="w-full text-sm"><thead className="bg-muted/60 text-left"><tr><th className="p-3">Event</th><th className="p-3">Magnitude</th><th className="p-3">Depth</th><th className="p-3">Time</th><th className="p-3">Risk</th></tr></thead><tbody>{events.map((event) => <tr key={event.id} className="border-t"><td className="p-3"><a className="font-medium hover:underline" href={event.url} target="_blank">{event.place}</a><div className="text-xs text-muted-foreground">{event.tsunami ? "Tsunami bulletin" : "No tsunami flag"}</div></td><td className="p-3">M {event.magnitude.toFixed(1)}</td><td className="p-3">{Math.round(event.depthKm)} km</td><td className="p-3">{formatDate(event.time)}</td><td className="p-3"><RiskBadge level={event.risk.level} /></td></tr>)}</tbody></table></div>; }
