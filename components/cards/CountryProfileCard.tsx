import type { CountryProfile } from "@/lib/types";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { RiskBadge } from "@/components/shared/RiskBadge";
export function CountryProfileCard({ country }: { country: CountryProfile }) { return <Card><CardHeader><div className="flex items-center justify-between"><CardTitle className="flex items-center gap-3"><img src={country.flag} alt="" className="h-8 w-12 rounded object-cover" />{country.name}</CardTitle><RiskBadge level={country.risk.level} /></div></CardHeader><CardContent className="grid gap-3 text-sm md:grid-cols-2"><div>Capital: {country.capital.join(", ") || "n/a"}</div><div>Population: {country.population.toLocaleString()}</div><div>Region: {country.region}</div><div>Currencies: {country.currencies.join(", ")}</div><div>Languages: {country.languages.slice(0,4).join(", ")}</div><div>Borders: {country.borders.length || "Island/none listed"}</div>{country.wikipedia ? <p className="md:col-span-2 text-muted-foreground">{country.wikipedia.extract}</p> : null}</CardContent></Card>; }
